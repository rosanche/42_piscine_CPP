#include <iostream>
#include <fstream>

void replace(char *file, char *search, char *replace)
{
    
}

int main(int ac, char **av)
{
    std::string sreplace; //= av[1] + ".replace";
    //sreplace  av[1] << 
    //std::ofstream replace(av[1] + ".replace");
}